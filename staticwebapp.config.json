{
    "routes": [
        {
            "route": "/",
            "methods":["PUT", "GET", "POST", "PATCH"],
            "rewrite": "/index.html"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}